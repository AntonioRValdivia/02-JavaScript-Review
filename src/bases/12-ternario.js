

const activo = true;

// let mensaje = '';

// if (activo){
//     mensaje = 'activo';
// }else{
//     mensaje = 'inactivo'
// }


// const mensaje = (activo) ? 'activo' : 'inactivo'; // Operador ternario

const mensaje = (activo) && 'activo'; // Operador condicional ternario
// Este operador es muy útil cuando no queremos que se ejecute el bloque de código 'else' por asi decirlo

console.log(mensaje);

